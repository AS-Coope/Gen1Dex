<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Adds AppBar but alters other parts of the app, like the view holders: Theme.MaterialComponents.DayNight.DarkActionBar-->
    <style name="Base.Theme.GenOneDex" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GenOneDex" parent="Base.Theme.GenOneDex" />
</resources>